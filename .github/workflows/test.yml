name: integration-test

on:
  push:

jobs:
  test:
    runs-on: ubuntu-22.04
    env:
      COMMENT: "/it --record"
      COMMENT2: "/it"
    steps:
      - run: |
          params=(${COMMENT:4})
          echo "${params[@]}"

          params2=(${COMMENT2:4})
          echo "${params[@]}"
